# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @nexservices/nexservices`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t nexservices-nexservices`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system nexservices-nexservices && \
          adduser --system -G nexservices-nexservices nexservices-nexservices

COPY apps/nexservices/dist nexservices-nexservices/
COPY apps/nexservices/package.json nexservices-nexservices/
RUN chown -R nexservices-nexservices:nexservices-nexservices .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix nexservices-nexservices --omit=dev -f install

CMD [ "node", "nexservices-nexservices" ]
